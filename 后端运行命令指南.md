# 🚀 玄机算命网 - 后端运行命令指南

## 📋 快速开始

### 1️⃣ 启动后端

```bash
cd /workspace/suanming-fix
./backend.sh start
```

**输出示例：**
```
Starting backend...
Backend started (PID: 54839)
```

---

### 2️⃣ 查看后端状态

```bash
./backend.sh status
```

**输出示例：**
```
Backend is running (PID: 54839)
```

---

### 3️⃣ 停止后端

```bash
./backend.sh stop
```

**输出示例：**
```
Stopping backend...
Backend stopped
```

---

### 4️⃣ 重启后端

```bash
./backend.sh restart
```

**功能：** 先停止后端，再启动后端

---

### 5️⃣ 查看后端日志

```bash
./backend.sh logs
```

**输出示例：**
```
=== Error Log ===
[2026-05-27 12:28:00] [INFO] Starting Gunicorn...
[2026-05-27 12:28:01] [INFO] Worker 54880 started...

=== Access Log ===
127.0.0.1 - - [27/May/2026] "GET /api/health HTTP/1.1" 200 -
127.0.0.1 - - [27/May/2026] "POST /api/fortune/bazi HTTP/1.1" 200 -
```

---

## 🔧 手动命令（不使用脚本）

### 启动后端（Gunicorn，生产环境）

```bash
cd /workspace/suanming-fix/api
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

**参数说明：**
- `-w 4`：启动4个worker进程（建议：CPU核心数 × 2 + 1）
- `-b 0.0.0.0:5000`：监听所有网络接口的5000端口
- `app:app`：`app.py` 文件中的 Flask 应用对象 `app`

**后台运行：**
```bash
nohup gunicorn -w 4 -b 0.0.0.0:5000 app:app > ../logs/gunicorn.log 2>&1 &
echo $! > ../backend.pid
```

---

### 启动后端（Flask 开发服务器）

```bash
cd /workspace/suanming-fix/api
python3 app.py
```

**注意：** 仅用于开发测试，不要用于生产环境！

---

### 停止后端

```bash
# 方法1：使用 PID 文件
kill $(cat /workspace/suanming-fix/backend.pid)

# 方法2：查找进程并停止
ps aux | grep gunicorn
kill <PID>

# 方法3：停止所有 Gunicorn 进程
pkill -9 gunicorn
```

---

### 查看后端状态

```bash
# 查看进程
ps aux | grep gunicorn

# 查看端口监听
netstat -tlnp | grep 5000
# 或
lsof -i:5000

# 测试健康检查端点
curl <INTERNAL_HOST_REMOVED>
```

**预期输出：**
```json
{
  "status": "ok",
  "service": "玄机算命网-大数据联网实时分析系统",
  "version": "2.0.0",
  "timestamp": "2026-05-27T12:30:00"
}
```

---

### 查看后端日志

```bash
# 查看错误日志（最后20行）
tail -n 20 /workspace/suanming-fix/logs/error.log

# 查看访问日志（最后20行）
tail -n 20 /workspace/suanming-fix/logs/access.log

# 实时查看日志
tail -f /workspace/suanming-fix/logs/*.log
```

---

## 🌐 访问地址

| 环境 | 地址 | 说明 |
|------|------|------|
| 本地 | `http://127.0.0.1:5000` | 本机访问 |
| 局域网 | `http://你的IP:5000` | 同一局域网内的其他设备访问 |
| 健康检查 | `http://127.0.0.1:5000/api/health` | API 健康检查端点 |

---

## 🔛 配置说明

### 修改监听端口

#### 方法1：修改 `backend.sh` 脚本
编辑 `backend.sh`，找到这一行：
```bash
nohup gunicorn -w 4 -b 0.0.0.0:5000 app:app > ...
```
改为：
```bash
nohup gunicorn -w 4 -b 0.0.0.0:8080 app:app > ...
```

#### 方法2：手动启动指定端口
```bash
cd /workspace/suanming-fix/api
gunicorn -w 4 -b 0.0.0.0:8080 app:app
```

---

### 修改 Worker 数量

**建议值：** `Worker数 = CPU核心数 × 2 + 1`

#### 查看 CPU 核心数
```bash
nproc
# 或
lscpu | grep "^CPU(s):"
```

#### 修改 `backend.sh` 脚本
找到这一行：
```bash
nohup gunicorn -w 4 -b 0.0.0.0:5000 app:app > ...
```
改为（例如 4核CPU → 9个workers）：
```bash
nohup gunicorn -w 9 -b 0.0.0.0:5000 app:app > ...
```

#### 手动启动指定 Worker 数
```bash
cd /workspace/suanming-fix/api
gunicorn -w 9 -b 0.0.0.0:5000 app:app
```

---

### 修改日志位置

编辑 `backend.sh`，修改这些行：
```bash
LOG_DIR="$APP_DIR/logs"
ACCESS_LOG="$LOG_DIR/access.log"
ERROR_LOG="$LOG_DIR/error.log"
```

改为：
```bash
LOG_DIR="/var/log/xuanji"
ACCESS_LOG="$LOG_DIR/access.log"
ERROR_LOG="$LOG_DIR/error.log"
```

然后创建日志目录：
```bash
sudo mkdir -p /var/log/xuanji
sudo chown $USER:$USER /var/log/xuanji
```

---

## 🐛 常见问题

### 1. 端口被占用

**错误信息：**
```
OSError: [Errno 98] Address already in use
```

**解决方法：**

#### 方法1：查找并停止占用端口的进程
```bash
# 查看占用端口的进程
lsof -i:5000

# 停止进程
kill -9 <PID>
```

#### 方法2：使用其他端口
```bash
cd /workspace/suanming-fix/api
gunicorn -w 4 -b 0.0.0.0:8080 app:app
```

#### 方法3：让系统自动分配端口
```bash
cd /workspace/suanming-fix/api
gunicorn -w 4 -b 0.0.0.0:0 app:app
```

---

### 2. 依赖包缺失

**错误信息：**
```
ModuleNotFoundError: No module named 'flask'
```

**解决方法：**
```bash
# backend.sh 会自动安装，或手动安装：
pip3 install flask flask-cors pyjwt pillow requests beautifulsoup4 gunicorn
```

---

### 3. 权限不足

**错误信息：**
```
Permission denied: '/workspace/suanming-fix/backend.pid'
```

**解决方法：**
```bash
# 添加执行权限
chmod +x backend.sh

# 或使用 sudo 运行
sudo ./backend.sh start
```

---

### 4. 后端无法访问

**排查步骤：**

#### 步骤1：检查后端是否运行
```bash
./backend.sh status
```

#### 步骤2：检查端口监听
```bash
netstat -tlnp | grep 5000
# 或
lsof -i:5000
```

#### 步骤3：测试健康检查端点
```bash
curl <INTERNAL_HOST_REMOVED>
```

#### 步骤4：查看错误日志
```bash
./backend.sh logs
# 或
tail -n 50 /workspace/suanming-fix/logs/error.log
```

---

### 5. 后端启动失败

**排查步骤：**

#### 步骤1：查看 Gunicorn 日志
```bash
cat /workspace/suanming-fix/logs/gunicorn.log
```

#### 步骤2：手动启动查看错误
```bash
cd /workspace/suanming-fix/api
python3 app.py
```

#### 步骤3：检查 Python 语法
```bash
python3 -m py_compile api/app.py
```

#### 步骤4：检查依赖包
```bash
python3 -c "import flask, flask_cors, jwt, PIL, requests, bs4"
```

---

## 📊 性能优化建议

### 1. Worker 数量优化

**公式：** `workers = CPU核心数 × 2 + 1`

**示例：**
- 1核CPU → 3个workers
- 2核CPU → 5个workers
- 4核CPU → 9个workers
- 8核CPU → 17个workers

**查看 CPU 核心数：**
```bash
nproc
```

**修改 Worker 数：**
```bash
# 编辑 backend.sh
vim backend.sh
# 找到 -w 4 改为 -w 9

# 或手动启动
cd /workspace/suanming-fix/api
gunicorn -w 9 -b 0.0.0.0:5000 app:app
```

---

### 2. 超时设置

**默认：** 30秒

**建议：** 120秒（处理复杂算命算法可能需要更长时间）

**修改方法：**
```bash
cd /workspace/suanming-fix/api
gunicorn --timeout 120 -w 4 -b 0.0.0.0:5000 app:app
```

---

### 3. 最大请求数（防止内存泄漏）

**建议：** 每个 worker 处理 1000 个请求后自动重启

**修改方法：**
```bash
cd /workspace/suanming-fix/api
gunicorn --max-requests 1000 --max-requests-jitter 100 \
    -w 4 -b 0.0.0.0:5000 app:app
```

---

### 4. 使用异步 Worker（高并发）

**安装 gevent：**
```bash
pip3 install gevent
```

**使用 gevent worker：**
```bash
cd /workspace/suanming-fix/api
gunicorn --worker-class gevent --workers 4 \
    -b 0.0.0.0:5000 app:app
```

---

## 🚀 生产环境部署

### 使用 Systemd 服务（推荐）

#### 1. 创建服务文件
```bash
sudo vim /etc/systemd/system/xuanji-backend.service
```

#### 2. 写入服务配置
```ini
[Unit]
Description=Xuanji Fortune Telling Backend
After=network.target

[Service]
Type=notify
User=root
Group=root
WorkingDirectory=/workspace/suanming-fix/api
ExecStart=/usr/bin/gunicorn \
    --workers 4 \
    --bind 0.0.0.0:5000 \
    --access-logfile /workspace/suanming-fix/logs/access.log \
    --error-logfile /workspace/suanming-fix/logs/error.log \
    --timeout 120 \
    app:app
ExecReload=/bin/kill -s HUP $MAINPID
KillMode=mixed
TimeoutStopSec=5
PrivateTmp=true

[Install]
WantedBy=multi-user.target
```

#### 3. 重新加载 Systemd
```bash
sudo systemctl daemon-reload
```

#### 4. 启动服务
```bash
sudo systemctl start xuanji-backend
```

#### 5. 查看服务状态
```bash
sudo systemctl status xuanji-backend
```

#### 6. 设置开机自启
```bash
sudo systemctl enable xuanji-backend
```

#### 7. 停止服务
```bash
sudo systemctl stop xuanji-backend
```

#### 8. 重启服务
```bash
sudo systemctl restart xuanji-backend
```

#### 9. 查看日志
```bash
sudo journalctl -u xuanji-backend -f
```

---

## 📞 技术支持

- **项目：** 玄机算命网
- **GitHub：** https://github.com/WERLK/suanming
- **Issues：** https://github.com/WERLK/suanming/issues

---

## 📋 完整命令列表

| 命令 | 说明 |
|------|------|
| `./backend.sh start` | 启动后端 |
| `./backend.sh stop` | 停止后端 |
| `./backend.sh restart` | 重启后端 |
| `./backend.sh status` | 查看状态 |
| `./backend.sh logs` | 查看日志 |
| `ps aux \| grep gunicorn` | 查看进程 |
| `lsof -i:5000` | 查看端口监听 |
| `curl <INTERNAL_HOST_REMOVED> | 测试健康检查 |
| `tail -f logs/*.log` | 实时查看日志 |

---

**文档版本：** v1.0  
**更新时间：** 2026年5月27日  
**作者：** WorkBuddy AI Agent  

---

**文档结束** ✅
